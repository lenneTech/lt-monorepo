{
  "name": "lt-monorepo",
  "version": "3.8.0",
  "description": "Monorepo starter kit",
  "author": "lenne.Tech <info@lenne.tech> (https://lenne.tech)",
  "license": "MIT",
  "scripts": {
    "c": "pnpm run check",
    "check": "node scripts/check.mjs",
    "check:raw": "NUXT_BUILD_DIR=.nuxt-check pnpm install --frozen-lockfile && NUXT_BUILD_DIR=.nuxt-check pnpm audit && pnpm run check:workspace && pnpm run check:pin && pnpm run check:playwright-image && pnpm run check:ci && pnpm run test:scripts && pnpm -r --parallel run check",
    "check:fix": "NUXT_BUILD_DIR=.nuxt-check pnpm install && NUXT_BUILD_DIR=.nuxt-check pnpm audit --fix && pnpm run check:workspace && pnpm run check:pin && pnpm run check:playwright-image && pnpm run check:ci && pnpm run test:scripts && pnpm -r --parallel run check:fix",
    "check:naf": "NUXT_BUILD_DIR=.nuxt-check pnpm install && pnpm run check:workspace && pnpm run check:pin && pnpm run check:playwright-image && pnpm run check:ci && pnpm run test:scripts && pnpm -r --parallel run check:naf",
    "check:workspace": "node scripts/check-workspace-consistency.mjs",
    "check:pin": "node scripts/check-packagemanager-pin.mjs",
    "check:playwright-image": "node scripts/check-playwright-image.mjs",
    "check:ci": "node scripts/check-ci-consistency.mjs",
    "test:scripts": "node --test scripts/build-test-gate.test.mjs scripts/nuxt-builddir-isolation.test.mjs scripts/check-ci-consistency.test.mjs scripts/mongo-watchdog.test.mjs scripts/guard-scripts.test.mjs",
    "cf": "pnpm run check:fix",
    "cnaf": "pnpm run check:naf",
    "clean": "pnpm -r --parallel run clean",
    "init": "pnpm install && pnpm -r --parallel run init",
    "dev": "lt dev up",
    "dev:down": "lt dev down",
    "dev:status": "lt dev status",
    "dev:doctor": "lt dev doctor",
    "start": "pnpm -r --parallel run start",
    "start:develop": "pnpm -r --parallel run start:develop",
    "start:test": "pnpm -r --parallel run start:test",
    "start:prod": "pnpm -r --parallel run start:prod",
    "build": "pnpm -r --parallel run build",
    "app:build": "pnpm -r --parallel run app:build",
    "build:develop": "pnpm -r --parallel run build:develop",
    "build:test": "pnpm -r --parallel run build:test",
    "build:prod": "pnpm -r --parallel run build:prod",
    "test": "pnpm -r --parallel run test",
    "api:test": "cd projects/api && pnpm run test:ci",
    "app:test": "cd projects/app && pnpm run test",
    "lint": "pnpm -r --parallel run lint",
    "lint:fix": "pnpm -r --parallel run lint:fix",
    "format": "pnpm -r --parallel run format",
    "format:check": "pnpm -r --parallel run format:check",
    "reinit": "rm -rf node_modules && rm -rf pnpm-lock.yaml && pnpm install && pnpm -r --parallel run reinit",
    "prepare": "husky || true",
    "release": "commit-and-tag-version",
    "release:minor": "commit-and-tag-version --release-as minor",
    "release:major": "commit-and-tag-version --release-as major"
  },
  "devDependencies": {
    "commit-and-tag-version": "12.7.3",
    "husky": "9.1.7"
  },
  "engines": {
    "node": ">= 22",
    "pnpm": "^11.0.0"
  },
  "packageManager": "pnpm@11.14.0+sha512.66c1ac4c7d4762d6d7dde44c7f3e5a73591ed0a0806e751d4ed32d4f004f25b2285a906b1fd8a9e3e621df3b4e2858bf88e50e0cf626bedbe977fe434a5caf85"
}
